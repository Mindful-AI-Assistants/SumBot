/ / https://github.com/bdougie/live/blob/main/•github/workflows/image-con
name: Compress Images on :
pull_request:
# Run Image Actions when JPG, JPEG, PNG or Webp files are added or c
# See https://help.github.com/en/actions/automating-your-workflow-wi
paths:
  - '**• jpg'
  - '**• jpeg'
  - '**• png'
  - '**•webp'
jobs:
build:
  # Only run on Pull Requests within the same repository, and not from
  if: github. event.pull
  _request. head. repo. full_name == github. repositc
name: calibreapp/image-actions runs-on: ubuntu-latest steps:
- name: Checkout Repo
uses: actions/checkout@v2
